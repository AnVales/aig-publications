name: Test schedule

on:
  schedule:
    - cron: "*/5 * * * *"
      timezone: "Europe/Madrid"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hola
        run: echo "HOLA - el schedule funciona"
